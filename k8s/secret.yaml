apiVersion: v1
kind: Secret
metadata:
  name: vita-wise-secrets
  namespace: vita-wise
type: Opaque
data:
  # Base64 encoded values - replace with your actual values
  # Use: echo -n "your-value" | base64
  SUPABASE_URL: '' # Replace with base64 encoded value
  SUPABASE_ANON_KEY: '' # Replace with base64 encoded value
  SUPABASE_SERVICE_ROLE_KEY: '' # Replace with base64 encoded value
  JWT_SECRET: '' # Replace with base64 encoded value
  OPENROUTER_API_KEY: '' # Replace with base64 encoded value
