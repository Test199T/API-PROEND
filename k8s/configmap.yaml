apiVersion: v1
kind: ConfigMap
metadata:
  name: vita-wise-config
  namespace: vita-wise
data:
  NODE_ENV: 'production'
  PORT: '8080'
  JWT_EXPIRES_IN: '24h'
  OPENROUTER_BASE_URL: 'https://openrouter.ai/api/v1'
  OPENROUTER_MODEL: 'gpt-4o-mini'
  # Add other non-sensitive configuration here
