# VITA WISE AI - Health Tracking Application API Implementation Guide

## 🚀 ภาพรวมระบบ

ระบบหลังบ้านของ VITA WISE AI ได้ถูก implement เรียบร้อยแล้วด้วย NestJS และ Supabase โดยมีฟีเจอร์หลักดังนี้:

### ✨ ฟีเจอร์หลักที่ implement แล้ว:

1. **Dashboard Service** - จัดการข้อมูลสรุปสุขภาพและสถิติ
2. **AI Service** - วิเคราะห์สุขภาพและให้คำแนะนำ
3. **Chat Service** - แชทกับ AI สำหรับคำแนะนำสุขภาพ
4. **Supabase Service** - เชื่อมต่อฐานข้อมูลและจัดการข้อมูล

## 🏗️ โครงสร้างระบบ

### 📁 Services
- `SupabaseService` - เชื่อมต่อฐานข้อมูล Supabase
- `AIService` - วิเคราะห์สุขภาพและสร้างคำแนะนำ
- `DashboardService` - จัดการข้อมูลแดชบอร์ด
- `ChatService` - จัดการการแชทกับ AI

### 📁 Controllers
- `DashboardController` - API endpoints สำหรับแดชบอร์ด
- `ChatController` - API endpoints สำหรับ AI Chat

### 📁 Entities
- `User` - ข้อมูลผู้ใช้
- `HealthGoal` - เป้าหมายสุขภาพ
- `FoodLog` - บันทึกอาหาร
- `ExerciseLog` - บันทึกการออกกำลังกาย
- `SleepLog` - บันทึกการนอน
- `WaterLog` - บันทึกการดื่มน้ำ
- `HealthMetric` - ข้อมูลสุขภาพ
- `ChatSession` - เซสชันแชท
- `ChatMessage` - ข้อความแชท
- `Notification` - การแจ้งเตือน
- `AIInsight` - ข้อมูลเชิงลึกจาก AI
- `UserPreference` - การตั้งค่าผู้ใช้

## 🔧 การติดตั้งและใช้งาน

### 1. ติดตั้ง Dependencies
```bash
npm install
```

### 2. ตั้งค่า Environment Variables
สร้างไฟล์ `.env` ในโฟลเดอร์หลักและเพิ่มค่าต่อไปนี้:

```env
# Supabase Configuration
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key

# Application Configuration
PORT=3000
NODE_ENV=development
```

### 3. รัน Application
```bash
# Development mode
npm run start:dev

# Production mode
npm run start:prod
```

## 📡 API Endpoints

### 🏠 Dashboard API

#### GET `/dashboard/overview`
- **Description**: ดึงข้อมูลภาพรวมของแดชบอร์ด
- **Response**: ข้อมูลสรุปสุขภาพประจำวัน, เมตริกสุขภาพ, เป้าหมาย, AI insights

#### GET `/dashboard/daily-summary/:date`
- **Description**: ดึงข้อมูลสรุปสุขภาพประจำวัน
- **Parameters**: `date` (YYYY-MM-DD)
- **Response**: ข้อมูลสรุปสุขภาพรายวัน

#### GET `/dashboard/weekly-trend/:weekStart`
- **Description**: ดึงข้อมูลแนวโน้มสุขภาพรายสัปดาห์
- **Parameters**: `weekStart` (YYYY-MM-DD)
- **Response**: ข้อมูลแนวโน้มสุขภาพรายสัปดาห์

#### GET `/dashboard/health-metrics`
- **Description**: ดึงข้อมูลเมตริกสุขภาพ
- **Response**: ข้อมูลสุขภาพปัจจุบัน, การเปลี่ยนแปลง, BMI, น้ำหนัก

#### GET `/dashboard/goals`
- **Description**: ดึงข้อมูลเป้าหมายสุขภาพ
- **Response**: เป้าหมายที่กำลังดำเนินการ, ความคืบหน้า

#### GET `/dashboard/statistics`
- **Description**: ดึงข้อมูลสถิติการใช้งาน
- **Parameters**: `days` (จำนวนวันที่ต้องการดู)
- **Response**: สถิติการใช้งาน, streak, ค่าเฉลี่ย

### 🍽️ Food Log API

#### GET `/dashboard/food-logs`
- **Description**: ดึงข้อมูลบันทึกอาหาร
- **Parameters**: `date` (optional, YYYY-MM-DD)
- **Response**: รายการอาหารที่รับประทาน

#### POST `/dashboard/food-logs`
- **Description**: สร้างบันทึกอาหารใหม่
- **Body**: ข้อมูลอาหาร (meal_type, food_name, calories, etc.)
- **Response**: ข้อมูลบันทึกอาหารที่สร้าง

### 🏃 Exercise Log API

#### GET `/dashboard/exercise-logs`
- **Description**: ดึงข้อมูลบันทึกการออกกำลังกาย
- **Parameters**: `date` (optional, YYYY-MM-DD)
- **Response**: รายการการออกกำลังกาย

#### POST `/dashboard/exercise-logs`
- **Description**: สร้างบันทึกการออกกำลังกายใหม่
- **Body**: ข้อมูลการออกกำลังกาย (exercise_name, duration_minutes, calories_burned, etc.)
- **Response**: ข้อมูลบันทึกการออกกำลังกายที่สร้าง

### 😴 Sleep Log API

#### GET `/dashboard/sleep-logs`
- **Description**: ดึงข้อมูลบันทึกการนอน
- **Parameters**: `date` (optional, YYYY-MM-DD)
- **Response**: รายการการนอน

#### POST `/dashboard/sleep-logs`
- **Description**: สร้างบันทึกการนอนใหม่
- **Body**: ข้อมูลการนอน (sleep_date, bedtime, wake_time, total_sleep_hours, etc.)
- **Response**: ข้อมูลบันทึกการนอนที่สร้าง

### 💧 Water Log API

#### GET `/dashboard/water-logs`
- **Description**: ดึงข้อมูลบันทึกการดื่มน้ำ
- **Parameters**: `date` (optional, YYYY-MM-DD)
- **Response**: รายการการดื่มน้ำ

#### POST `/dashboard/water-logs`
- **Description**: สร้างบันทึกการดื่มน้ำใหม่
- **Body**: ข้อมูลการดื่มน้ำ (amount_ml, drink_type, consumed_at, etc.)
- **Response**: ข้อมูลบันทึกการดื่มน้ำที่สร้าง

### 🤖 AI Insights API

#### GET `/dashboard/ai-insights`
- **Description**: ดึงข้อมูลเชิงลึกจาก AI
- **Response**: insights และคำแนะนำจาก AI

#### GET `/dashboard/ai-recommendations`
- **Description**: ดึงคำแนะนำจาก AI
- **Response**: คำแนะนำที่ปรับแต่งตามข้อมูลสุขภาพของผู้ใช้

#### POST `/dashboard/ai-analyze`
- **Description**: วิเคราะห์สุขภาพของผู้ใช้
- **Response**: ผลการวิเคราะห์สุขภาพ

### 💬 Chat API

#### POST `/chat/sessions`
- **Description**: สร้างเซสชันแชทใหม่
- **Body**: `{ title?: string }`
- **Response**: ข้อมูลเซสชันแชท

#### GET `/chat/sessions`
- **Description**: ดึงเซสชันแชททั้งหมดของผู้ใช้
- **Response**: รายการเซสชันแชท

#### GET `/chat/sessions/:sessionId`
- **Description**: ดึงข้อความในเซสชันแชท
- **Parameters**: `sessionId`
- **Response**: ข้อความทั้งหมดในเซสชัน

#### POST `/chat/sessions/:sessionId/messages`
- **Description**: ส่งข้อความไปยัง AI
- **Body**: `{ message: string }`
- **Response**: การตอบกลับจาก AI

#### POST `/chat/ai/analyze`
- **Description**: วิเคราะห์ข้อความของผู้ใช้
- **Body**: `{ message: string, context?: any }`
- **Response**: การวิเคราะห์และคำแนะนำ

#### POST `/chat/ai/health-advice`
- **Description**: ขอคำแนะนำสุขภาพจาก AI
- **Body**: `{ topic: string, specificQuestion?: string }`
- **Response**: คำแนะนำสุขภาพ

#### GET `/chat/quick-responses`
- **Description**: ดึงคำถามที่พบบ่อย
- **Response**: รายการคำถามที่พบบ่อย

#### POST `/chat/quick-responses/:responseId/ask`
- **Description**: ถามคำถามที่พบบ่อย
- **Parameters**: `responseId`
- **Response**: คำตอบจาก AI

## 🔐 Authentication

ระบบใช้ `AuthGuard` สำหรับการป้องกัน API endpoints ทั้งหมด ผู้ใช้ต้องมี JWT token ที่ถูกต้องในการเข้าถึง

### การใช้งาน AuthGuard:
```typescript
@UseGuards(AuthGuard)
@Controller('dashboard')
export class DashboardController {
  // ... endpoints
}
```

## 📊 ข้อมูลที่ส่งกลับ

### Response Format:
```typescript
{
  success: boolean;
  data?: any;
  message?: string;
  error?: string;
}
```

### Error Handling:
```typescript
{
  success: false;
  error: "Error message description"
}
```

## 🧠 AI Features

### 1. Health Analysis
- วิเคราะห์โภชนาการ (Nutrition Score)
- วิเคราะห์การออกกำลังกาย (Exercise Score)
- วิเคราะห์การนอน (Sleep Score)
- วิเคราะห์การดื่มน้ำ (Water Score)
- คำนวณคะแนนรวม (Overall Score)

### 2. AI Recommendations
- คำแนะนำด้านโภชนาการ
- คำแนะนำด้านการออกกำลังกาย
- คำแนะนำด้านการนอน
- คำแนะนำด้านการดื่มน้ำ
- คำแนะนำด้านเป้าหมายสุขภาพ

### 3. AI Chat
- แชทกับ AI สำหรับคำถามสุขภาพ
- คำแนะนำที่ปรับแต่งตามข้อมูลผู้ใช้
- การวิเคราะห์ข้อความแบบ real-time
- คำถามที่พบบ่อย (Quick Responses)

## 📈 Dashboard Features

### 1. Daily Health Summary
- แคลอรี่ที่รับประทาน
- แคลอรี่ที่เผาผลาญ
- ปริมาณน้ำที่ดื่ม
- ชั่วโมงการนอน
- คุณภาพการนอน
- คะแนนสุขภาพแต่ละด้าน

### 2. Weekly Health Trends
- แนวโน้มสุขภาพรายสัปดาห์
- ค่าเฉลี่ยรายสัปดาห์
- การเปรียบเทียบกับสัปดาห์ก่อน

### 3. Health Metrics
- น้ำหนักปัจจุบัน
- BMI
- การเปลี่ยนแปลงน้ำหนัก
- เปอร์เซ็นต์ไขมัน
- มวลกล้ามเนื้อ
- อัตราการเต้นหัวใจ
- ความดันโลหิต

### 4. Goals Progress
- เป้าหมายที่กำลังดำเนินการ
- ความคืบหน้าเป้าหมาย
- วันเหลือในการบรรลุเป้าหมาย
- สถานะเป้าหมาย

## 🚀 การพัฒนาต่อ

### 1. AI Integration
- เชื่อมต่อกับ AI Model จริง (Claude, GPT, etc.)
- เพิ่มความสามารถในการวิเคราะห์ข้อมูล
- ปรับปรุงความแม่นยำของคำแนะนำ

### 2. Real-time Features
- WebSocket สำหรับการแจ้งเตือนแบบ real-time
- Live updates ของข้อมูลสุขภาพ
- Real-time chat

### 3. Advanced Analytics
- Machine Learning models
- Predictive analytics
- Health trend forecasting

### 4. Mobile App
- React Native หรือ Flutter app
- Push notifications
- Offline support

## 🧪 Testing

### Run Tests:
```bash
# Unit tests
npm run test

# E2E tests
npm run test:e2e

# Test coverage
npm run test:cov
```

## 📝 Notes

1. **Database**: ระบบใช้ Supabase เป็นฐานข้อมูลหลัก
2. **Authentication**: ใช้ JWT tokens สำหรับการยืนยันตัวตน
3. **AI**: ปัจจุบันใช้การจำลอง AI responses (สามารถเชื่อมต่อกับ AI service จริงได้ในอนาคต)
4. **Error Handling**: มีระบบจัดการ error ที่ครอบคลุม
5. **Validation**: ใช้ class-validator สำหรับการตรวจสอบข้อมูล

## 🔗 Useful Links

- [NestJS Documentation](https://docs.nestjs.com/)
- [Supabase Documentation](https://supabase.com/docs)
- [TypeScript Documentation](https://www.typescriptlang.org/docs/)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)

## 📞 Support

หากมีคำถามหรือต้องการความช่วยเหลือ กรุณาติดต่อทีมพัฒนา VITA WISE AI
