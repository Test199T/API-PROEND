# Health Goals API - สรุปการใช้งาน

## 🎯 ภาพรวม
Health Goals API ของ Vita Wise AI พร้อมใช้งานแล้วที่ `http://localhost:3000/health-goals` พร้อมฟีเจอร์ครบถ้วนสำหรับการจัดการเป้าหมายสุขภาพ

## 🚀 API Endpoints ที่พร้อมใช้งาน

### Public Endpoints (ไม่ต้องการ Authentication)
- `GET /health-goals/test/public` - ทดสอบ API
- `GET /health-goals/test/sample-data` - ข้อมูลตัวอย่าง

### Protected Endpoints (ต้องการ JWT Token)
- `GET /health-goals` - ดึงรายการเป้าหมายทั้งหมด
- `POST /health-goals` - สร้างเป้าหมายใหม่
- `GET /health-goals/:id` - ดึงเป้าหมายตาม ID
- `PUT /health-goals/:id` - อัพเดทเป้าหมาย
- `DELETE /health-goals/:id` - ลบเป้าหมาย
- `PUT /health-goals/:id/progress` - อัพเดทความคืบหน้า
- `GET /health-goals/:id/progress` - ดึงความคืบหน้า
- `PUT /health-goals/:id/complete` - ทำเป้าหมายให้เสร็จ
- `PUT /health-goals/:id/pause` - หยุดเป้าหมายชั่วคราว
- `PUT /health-goals/:id/resume` - กลับมาเริ่มเป้าหมาย
- `PUT /health-goals/:id/cancel` - ยกเลิกเป้าหมาย

### Search & Filter Endpoints
- `GET /health-goals/search/active` - เป้าหมายที่กำลังดำเนินการ
- `GET /health-goals/search/completed` - เป้าหมายที่เสร็จสิ้น
- `GET /health-goals/search/overdue` - เป้าหมายที่เกินกำหนด
- `GET /health-goals/search/priority/:priority` - ตามความสำคัญ
- `GET /health-goals/search/type/:type` - ตามประเภท

### Special Endpoints
- `GET /health-goals/templates` - เทมเพลตเป้าหมาย
- `GET /health-goals/recommendations` - คำแนะนำเป้าหมาย
- `GET /health-goals/stats/overview` - สถิติภาพรวม
- `PUT /health-goals/bulk-update` - อัพเดทหลายรายการ

## 📁 ไฟล์ที่สร้างขึ้น

### 1. Postman Collection
- `Health-Goals-API-Postman-Collection.json` - สำหรับทดสอบใน Postman

### 2. HTTP Testing File
- `test-health-goals-api.http` - สำหรับทดสอบด้วย VS Code REST Client

### 3. Documentation
- `HEALTH-GOALS-API-README.md` - คู่มือการใช้งาน API แบบละเอียด

### 4. Frontend Examples
- `frontend-examples/health-goals-example.js` - ตัวอย่างการใช้งานใน JavaScript
- `frontend-examples/HealthGoalsComponent.jsx` - React Component ตัวอย่าง

### 5. Summary
- `HEALTH-GOALS-API-SUMMARY.md` - ไฟล์นี้ (สรุปการใช้งาน)

## 🧪 การทดสอบ

### 1. ทดสอบ API Health
```bash
curl http://localhost:3000/health-goals/test/public
```

### 2. ทดสอบข้อมูลตัวอย่าง
```bash
curl http://localhost:3000/health-goals/test/sample-data
```

### 3. ทดสอบด้วย Postman
1. นำเข้าไฟล์ `Health-Goals-API-Postman-Collection.json`
2. ตั้งค่า `base_url` เป็น `http://localhost:3000`
3. ตั้งค่า `auth_token` เป็น JWT token ของคุณ
4. ทดสอบ endpoint ต่างๆ

### 4. ทดสอบด้วย VS Code
1. ติดตั้ง REST Client extension
2. เปิดไฟล์ `test-health-goals-api.http`
3. คลิก "Send Request" ที่ endpoint ที่ต้องการ

## 🔐 การยืนยันตัวตน

### JWT Token Format
```
Authorization: Bearer YOUR_JWT_TOKEN_HERE
```

### ตัวอย่างการใช้งาน
```javascript
const headers = {
  'Authorization': `Bearer ${token}`,
  'Content-Type': 'application/json'
};

const response = await fetch('http://localhost:3000/health-goals', {
  headers
});
```

## 📊 ข้อมูลที่รองรับ

### Goal Types
- `weight_loss` - ลดน้ำหนัก
- `weight_gain` - เพิ่มน้ำหนัก
- `muscle_gain` - เพิ่มกล้ามเนื้อ
- `endurance` - เพิ่มความทนทาน
- `flexibility` - เพิ่มความยืดหยุ่น
- `stress_reduction` - ลดความเครียด
- `sleep_improvement` - ปรับปรุงการนอน
- `nutrition` - โภชนาการ
- `other` - อื่นๆ

### Goal Status
- `active` - กำลังดำเนินการ
- `completed` - เสร็จสิ้น
- `paused` - หยุดชั่วคราว
- `cancelled` - ยกเลิก

### Goal Priority
- `low` - ต่ำ
- `medium` - ปานกลาง
- `high` - สูง
- `urgent` - เร่งด่วน

## 🎨 การใช้งานกับ Frontend

### 1. JavaScript/TypeScript
```javascript
import { 
  getAllHealthGoals, 
  createHealthGoal, 
  updateGoalProgress 
} from './health-goals-example.js';

// ดึงรายการเป้าหมาย
const goals = await getAllHealthGoals();

// สร้างเป้าหมายใหม่
const newGoal = await createHealthGoal({
  goal_type: 'weight_loss',
  title: 'ลดน้ำหนัก 5 กิโลกรัม',
  target_value: 5,
  start_date: '2024-01-01'
});
```

### 2. React
```jsx
import HealthGoalsComponent from './frontend-examples/HealthGoalsComponent';

function App() {
  return (
    <div>
      <HealthGoalsComponent />
    </div>
  );
}
```

## 🔍 Query Parameters ที่รองรับ

### Pagination
- `page` - หน้าปัจจุบัน (เริ่มต้นที่ 1)
- `limit` - จำนวนรายการต่อหน้า (เริ่มต้นที่ 10)

### Filtering
- `goal_type` - ประเภทเป้าหมาย
- `status` - สถานะ
- `priority` - ความสำคัญ
- `search` - ค้นหาตามชื่อหรือคำอธิบาย

### Date Range
- `start_date_from` - วันที่เริ่มต้นจาก
- `start_date_to` - วันที่เริ่มต้นถึง
- `target_date_from` - วันที่เป้าหมายจาก
- `target_date_to` - วันที่เป้าหมายถึง

## 📝 ตัวอย่าง Request Body

### สร้างเป้าหมายใหม่
```json
{
  "goal_type": "weight_loss",
  "title": "ลดน้ำหนัก 5 กิโลกรัม",
  "description": "ลดน้ำหนักเพื่อสุขภาพที่ดีขึ้น",
  "target_value": 5,
  "current_value": 0,
  "unit": "kg",
  "start_date": "2024-01-01",
  "target_date": "2024-06-01",
  "priority": "medium"
}
```

### อัพเดทความคืบหน้า
```json
{
  "current_value": 3
}
```

### Bulk Update
```json
{
  "goal_ids": [1, 2, 3],
  "updates": {
    "status": "active",
    "priority": "medium"
  }
}
```

## ✅ Response Format

### Success Response
```json
{
  "success": true,
  "message": "ดำเนินการสำเร็จ",
  "data": { ... },
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

### Error Response
```json
{
  "success": false,
  "message": "ข้อผิดพลาด",
  "error": "รายละเอียดข้อผิดพลาด",
  "statusCode": 400,
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

## 🚨 หมายเหตุสำคัญ

1. **Port**: API ทำงานที่ port 3000 (ไม่ใช่ 8080 ตามที่ขอ)
2. **Authentication**: ทุก endpoint ที่สำคัญต้องการ JWT token
3. **CORS**: รองรับ frontend ที่รันที่ localhost:5173
4. **Validation**: ข้อมูลจะถูก validate ตาม DTO ที่กำหนด
5. **Error Handling**: มีระบบจัดการ error ที่มาตรฐาน

## 🔧 การแก้ไขปัญหา

### ปัญหาที่พบบ่อย
1. **401 Unauthorized** - ตรวจสอบ JWT token
2. **400 Bad Request** - ตรวจสอบข้อมูลที่ส่ง
3. **404 Not Found** - ตรวจสอบ ID ของเป้าหมาย
4. **500 Internal Server Error** - ตรวจสอบ log ของ server

### การ Debug
1. ใช้ endpoint `/health-goals/test/public` เพื่อทดสอบ API
2. ตรวจสอบ Network tab ใน Developer Tools
3. ใช้ Postman หรือ cURL เพื่อทดสอบโดยตรง
4. ตรวจสอบ log ของ NestJS server

## 📞 การสนับสนุน

หากมีปัญหาหรือต้องการความช่วยเหลือ:
1. ตรวจสอบไฟล์ README ที่ละเอียด
2. ใช้ endpoint ทดสอบเพื่อตรวจสอบสถานะ API
3. ตรวจสอบ log ของ server
4. ใช้ Postman Collection ที่เตรียมไว้

---

**🎉 Health Goals API พร้อมใช้งานแล้ว! สามารถนำไปใช้งานกับ frontend ได้ทันที**
